"use client";

/**
 * Cart drawer — slides in from the right (desktop) / bottom (mobile).
 * Shows item list, quantity controls (+/-), remove, total, and checkout.
 * This is the same component as CartPopup; exported as CartDrawer for the requested API.
 */

export { CartPopup as CartDrawer } from "@/components/cart/cart-popup";
