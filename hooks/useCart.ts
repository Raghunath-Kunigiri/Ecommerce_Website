/**
 * Cart hook — re-exports store and popup for a single import.
 * Use for cart state + opening the drawer.
 */

export { useCart } from "@/lib/store/cart";
export { useCartPopup } from "@/components/cart/cart-popup-context";
